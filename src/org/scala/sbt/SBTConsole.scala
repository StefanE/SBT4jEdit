package org.scala.sbt

import console.Console
import org.gjt.sp.jedit.View
import java.awt.Color
import java.awt.Color._
   /*
class SBTConsole(view: View) extends Console(view) {

  private val infoColor = GREEN
  private val warningColor = YELLOW
  private val errorColor = RED
  private val plainColor = BLACK

  override def getPlainColor() = plainColor

  override def getInfoColor() = infoColor

  override def getWarningColor() = warningColor

  override def getErrorColor() = errorColor


}     */